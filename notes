pandas in an external library: must install

first column of table is the categories/lables of data

pandas allows us to read data
just use functions in the pandas library



if sequecne of print sequence, but i can also jsut print
if last one, print and overwrites the previous ones
